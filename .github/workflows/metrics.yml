name: Generate GitHub Metrics

on:
  schedule:
    - cron: "0 2 * * *"   # diario 02:00 UTC
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  metrics:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Generate metrics.svg
        uses: lowlighter/metrics@latest
        with:
          # token: un Personal Access Token (classic) con scope 'repo'.
          # Guárdalo como secret del repo: METRICS_TOKEN. Ver ../SETUP-WIDGETS.md
          token: ${{ secrets.METRICS_TOKEN }}
          filename: github-metrics.svg
          user: ${{ github.repository_owner }}
          template: classic
          base: header, activity, community, repositories, metadata

          # --- look terminal/verde + plugins de alto impacto ---
          config_timezone: America/Lima
          config_order: base.header, isocalendar, languages, activity

          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          plugin_languages: yes
          plugin_languages_details: bytes-size, percentage
          plugin_languages_limit: 8

      # Achievements card (reemplaza al trophy externo, que estaba caído)
      - name: Generate achievements.svg
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics-achievements.svg
          user: ${{ github.repository_owner }}
          template: classic
          base: ""
          config_timezone: America/Lima
          plugin_achievements: yes
          plugin_achievements_threshold: C
          plugin_achievements_secrets: yes
          plugin_achievements_display: compact
          plugin_achievements_limit: 0
